export const dynamic="force-dynamic";
export async function GET(req:Request){const env=(await import("cloudflare:workers")).env,name=new URL(req.url).searchParams.get("name");if(!name)return new Response("Foto não encontrada",{status:404});const row=await env.DB.prepare("SELECT object_key,content_type FROM profiles WHERE name_key=?").bind(name).first<{object_key:string;content_type:string}>();if(!row)return new Response("Foto não encontrada",{status:404});const object=await env.BUCKET.get(row.object_key);if(!object)return new Response("Foto não encontrada",{status:404});return new Response(object.body,{headers:{"Content-Type":row.content_type,"Cache-Control":"private, max-age=3600"}})}
